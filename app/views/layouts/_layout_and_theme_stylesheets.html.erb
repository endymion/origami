<%  theme = @theme || THEME
	if theme.nil? %>
	<%= body %>
<% else %>
	<%= stylesheet_link_tag "layouts/#{layout}" %>
	<%= stylesheet_link_tag theme %>
<% end %>
<% unless WIDTH.nil? %>
	<style>
		#page {
			width: <%= WIDTH %>;
			margin-left: auto;
			margin-right: auto;
		}
	</style>
<% end %>
